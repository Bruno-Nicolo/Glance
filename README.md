# Glance

Glance is an experimental macOS eye-tracking interaction system using the built-in webcam.

The first goal is to validate gaze-to-cursor interaction with a custom overlay cursor, a Python vision core, and a small native macOS helper. See `docs/adr/0001-glance-mvp-architecture.md` for the initial architecture and MVP scope.
